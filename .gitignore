# data shards (downloaded via data/cached_fineweb10B.py)
data/fineweb10B/
*.bin

# experiment outputs (results/ holds the curated, committed ones; everything else is scratch)
*.db
inductor_cache/
__pycache__/
*.pyc

# scratch experiment JSONs at the repo root (curated results live in results/)
/bo_*.json
/grid*.json
/results_*.json

# internal working doc — kept locally, not part of the public repo
/SIMPLIFICATION_PLAN.md

# local dev equivalence probe
/_fingerprint.py

# parked: bug story + findings (kept locally, re-addable later)
/BUGLOG.md
/figures/*
!/figures/to_target.png
/results/
